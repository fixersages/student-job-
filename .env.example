# 必填：复制本文件为 .env 并填写（本地 npm run dev / npm run build 都会读取）
# 切勿将 .env 提交到 Git。Vercel：Project → Settings → Environment Variables 添加同名两项。
# 未配置时应用会在启动阶段直接报错，避免误连错误环境或把密钥写进仓库。

# 须为 https，且仅为项目根域名（不要写成 …/rest/v1、不要尾部 /）；写错会导致登录报「无法连上 Supabase / Failed to fetch」
VITE_SUPABASE_URL=https://你的项目ref.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon公钥

# 可选：OAuth 回调根地址（须与 Supabase → Authentication → URL Configuration → Redirect URLs 完全一致，不要带 #/）
# 不填时：本地 npm run dev 用当前 origin；生产构建默认 https://student-job-three.vercel.app
# VITE_AUTH_REDIRECT_URL=https://你的预览或自定义域名
