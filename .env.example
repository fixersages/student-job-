# 必填：复制本文件为 .env 并填写（本地 npm run dev / npm run build 都会读取）
# 切勿将 .env 提交到 Git。Vercel：Project → Settings → Environment Variables 添加同名两项。
# 未配置时应用会在启动阶段直接报错，避免误连错误环境或把密钥写进仓库。

VITE_SUPABASE_URL=https://你的项目ref.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon公钥
